#app.container
  h1.title
    | JS Diff test
  .columns
    .column
      h3.subtitle
        | Version1
      .content
        textarea.textarea(v-model="str1")
    .column
      h3.subtitle
        | Version2
      .content
        textarea.textarea(v-model="str2")
  .diff
    h3.subtitle
      | VersionDiff
    .diff-content(v-html="strDiff")
